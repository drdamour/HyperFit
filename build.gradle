apply plugin: 'java'

sourceCompatibility = 1.5
version = '1.0'

repositories {
    mavenCentral()
}

dependencies {
    //We are built on top of retrofit
    compile 'com.squareup.retrofit:retrofit:1.7.1'

    testCompile group: 'junit', name: 'junit', version: '4.11'
}